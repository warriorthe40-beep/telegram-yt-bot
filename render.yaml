# Render Blueprint - Docker Deployment
# Using Docker to ensure FFmpeg is installed

services:
  - type: web
    name: youtube-telegram-bot
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: BOT_TOKEN
        sync: false
    healthCheckPath: /health
